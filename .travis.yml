language: rust
matrix:
  include:
    - os: linux
      rust: stable
      dist: trusty
    - os: linux
      rust: nightly
      dist: trusty
    - os: osx
      rust: stable
    - os: osx
      rust: nightly
script:
  - rustc --version
  - cargo +nightly install || touch clippy_failed
  - RUST_BACKTRACE=1 cargo build
  - if [ ! -f clippy_failed ]; then cargo +nightly clippy; fi
  - cd examples
  - RUST_BACKTRACE=1 cargo build
  - if [ ! -f clippy_failed ]; then cargo +nightly clippy; fi
